extends layout.pug

block content
  #main Sto a parti'...
  #editor
    - if (message)
      h2= message
    - else
      h2 Inserisci una frase da sparare
    p Non possiamo dirti quando la tua frase verra' sparata, ma verra' sparata.
    - url = "/sendOne?room=666"
    form(action=url method="post")
      input(type="hidden" name="room" value=room)
      textarea#text-edit(cols=50 rows=3 name="text")
      br
      input(type="submit" value="Invia")
  button#gfs(onclick="$(document).fullScreen(true);$('#gfs').remove();")
    Fullscreen
